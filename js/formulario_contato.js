function Enviar() {
 
    var nome = document.getElementById("nomeid");
 
    if (nome.value != "") {
        alert('Dados enviados com sucesso!! Responderemos em breve, desde já gratos pelo contato.');
    }
 
}
